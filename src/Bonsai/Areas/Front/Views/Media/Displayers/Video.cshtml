@using Bonsai.Code.Utils.Helpers
@model Bonsai.Areas.Front.ViewModels.Media.MediaVM

@if(Model.IsProcessed)
{
    <div class="video-wrapper">
        <video class="media-video" controls poster="@Url.Content(Model.PreviewPath)" preload="auto">
            <source src="@Url.Content(Model.OriginalPath)" type="video/mp4" />
            <div class="alert alert-warning">The video is not supporder in your browser.</div>
        </video>
    </div>
}
else
{
    <div class="photo-wrapper">
        <img src="@Url.MediaUrl(Model)" class="media-photo" alt="Processing the video..." />
    </div>
    <div class="alert alert-info">
        Video will be available after the processing succeded. Please reload the page.browser
    </div>
}